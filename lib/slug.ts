// URL-safe slug from an arbitrary label. "Sunday Main" -> "sunday-main".
export function slugify(input: string): string {
  return input
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
